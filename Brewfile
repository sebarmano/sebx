# Brewfile — managed by sebx. Run: brew bundle --file=Brewfile
# Add formulae/casks here and re-run 02-brew.sh (or brew bundle).

# ── CLI tools ──────────────────────────────────────────────────────────────
brew "git"
brew "gh"            # GitHub CLI
brew "fzf"           # fuzzy finder
brew "ripgrep"       # fast grep (rg)
brew "fd"            # fast find
brew "bat"           # better cat (with syntax highlight)
brew "jq"            # JSON processor
brew "eza"           # modern ls
brew "zoxide"        # smarter cd
brew "tmux"
brew "neovim"
brew "stow"          # dotfile symlink manager
brew "mise"          # language version manager
brew "starship"      # cross-shell prompt
brew "wget"
brew "tree"

# ── GUI apps (casks) ────────────────────────────────────────────────────────
cask "ghostty"            # terminal
cask "visual-studio-code"
cask "rectangle"          # window management

# Uncomment / add your browser and other apps:
# cask "arc"
# cask "firefox"
# cask "1password"
# cask "raycast"
# cask "obsidian"
# cask "spotify"

# ── Mac App Store (requires: brew "mas") ────────────────────────────────────
# brew "mas"
# mas "Xcode", id: 497799835
